{
  "name": "klipa",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "backend/**/*.ts": "npm --prefix backend run lint",
    "frontend/**/*.{ts,vue}": "npm --prefix frontend run lint"
  }
}
